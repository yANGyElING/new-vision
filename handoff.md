# Handoff

> 更新于 2026-08-29。此文件**就地更新**，不追加历史条目。

## 1. 当前目标与停止点

**目标**：为「中心-自治节点视频平台」逐个领域完成设计文档，**当前阶段只做设计，不写实现代码**。

**已完成并定稿**（2026-08-28 与用户逐条确认，全部 ⏳ 未实施）：

- [`docs/design/permission-model.md`](docs/design/permission-model.md) —— 数据权限、功能权限、租户套餐、权限点清单、审计、不变量 I1–I10、决策 D11–D21 / D28–D39
- [`docs/design/identity-and-auth.md`](docs/design/identity-and-auth.md) —— 租户与账号生命周期、身份标识、登录方式、密码、会话与限流、决策 D22–D27 / D30 / D32 / D35 / D40
- [`docs/design/identity-scope-refactor.md`](docs/design/identity-scope-refactor.md) —— 已回填为「已实现」，§5.1 记录 4 条被后续文档推翻的决策及理由

**停止点**：下一个课题已选定并写好讨论提纲，**尚未开始讨论**。

## 2. 下一步：Catalog 与通道模型

课题背景、既有约束、待讨论问题清单全部在 **[`docs/design/catalog-channel-model.md`](docs/design/catalog-channel-model.md)**（状态 ⏳ 待讨论，不含任何已确定的设计）。此处不重复。

三个真岔路：通道的身份（§4.1）、设备与平台两个权威打架（§4.2）、通道消失怎么办（§4.3）。

**建议的第一步**：派**一个**探查 agent 摸清现有 outbox 同步链路、Kamailio gb28181 模块已具备的能力、Redis 运行时投影的键结构；同时把第一轮能问的问题抛给用户。事实自己查，决策交用户。

## 3. 建议调用的技能

| 技能 | 用途 |
|---|---|
| `.emma/skills/brainstorm` | 主方法。按决策树分轮提问：每轮把**前置已定**的问题一次问完，编号 + 给出推荐答案，等用户回答后重算前沿 |
| `.emma/skills/handoff` | 会话结束时就地更新本文件 |
| `.emma/skills/review` | 若进入实现阶段后使用 |

## 4. 与用户协作的非显然约束

- **必须用具体场景解释，不能用术语。** 抽象表述用户会直接说「没懂」。本项目已建立一套固定人物：租户「福州水务集团 / 闽运物流」，组织树「鼓楼片区 → 五凤泵站 / 洪山桥泵站、台江片区、第一水厂」，人物「小陈（平台运维）/ 老张（租户管理员）/ 小李（片区主管）/ 小王（门卫）」。**沿用它们**，不要另起炉灶。
- **用户的设计风格：满足需求前提下选最简单的。** 遇到无实质差异的岔路他会直接交给 agent 裁决，并以此为判据。注意：需求已被他明确提出过时，「最简单」指实现该需求的最简方案，不是砍需求。
- **一个领域一个专题文档 + 两处索引必须同步更新**（`docs/README.md` 与 `docs/design/README.md`）。决策编号 D 跨文档连续；后文推翻前文必须在前文写明推翻者与理由，不得静默改写。
- **不要并行派大量 subagent。** 本会话有一次教训：一个宽口径的联网调研任务被 agent 自行拆成 4 个主题组 + 8 个产品组共 12 个并发，全部因共享网页抓取配额而 429 退避，一小时零产出。**规则：单 agent、明确禁止再分身、题目收窄成一句话、给时间上限。** 仓库内的封闭事实查证（去指定文件找指定事实）则很高效，几分钟出结果。
- 派生的 agent 有 `in_process_teammate` 类型，在 agent 列表里显示为 `roster` 而非 `running`，**看起来像残留但其实是活的**。清理时必须逐个 `TaskStop` 后复查列表。

## 5. 阻塞与已验证的事实

- **Docker daemon 缺失**：本机只有 CLI/Compose，无法构建镜像或做容器内验收。Kamailio 自定义 C 模块**代码就绪但从未编译**，SIP/RPC 闭环从未端到端验证。任何依赖真实 PostgreSQL 或容器运行的结论都无法在本机确认。
- **Catalog / 通道完全未实现**：全仓库对 `channel|catalog|通道` 零匹配。设备模型止于注册与鉴权（`internal/nodeapp/device/device.go:39-62`）。
- **已上线代码里有 4 个安全缺陷**，与上述重设计无依赖，可独立先修，详见 `permission-model.md` §9.1 的 A/B/D/E，对应不变量 I1/I2/I3/I4：降级用户凭旧 token 仍有全租户可见性；停用账号在请求路径上完全不生效；设备与组织的跨租户一致性零校验；`PATCH /api/v1/devices/{id}` 注册在认证守卫之外。
- **性能真瓶颈已定位**：认证中间件每请求拉取整租户用户列表 + N+1 查询（`internal/nodeapp/app.go:72-82`），量级远大于此前讨论的组织子树展开。
- **`docs/knowledge-base.md` 已过期**：§5 API 表面与 §6 数据模型仍停留在 `org_units` 重构之前，不含身份/权限任何内容。引用它时需先核对代码。
- **未提交**：本轮全部文档改动尚在 working tree，未 commit。
